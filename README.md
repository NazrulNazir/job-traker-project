## ans no qustion no.1

getElementById, getElementsByClassName, and querySelector / querySelectorAll differant beetween:- getElementById can only capture a singla element through a unique ID, class can capture one or more elements though a className, and qurySelector / qurySelectorAll cam capture everything, be it a tagelement,className or ID.

Id and className return htmlElements and qureySelector / qurySelectorAll  nodes;


## ans no qustion no.2

In the dom I can create a new element using getElement and insert in into the parent element using  appentChind(parentElement);

## ans no qustion no.3

When an event accurs on an element, it first acts on that elements and then goes up to all its parents.This how bubbles word.